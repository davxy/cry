image: davxy/sonar-builder

stages:
    - deploy

# Publish API Documentation
pages:
    stage: deploy
    script:
        - make doc
        - mv doc/doxy/html/ public/
        - pwd
        - echo ~
        - ls -al
        - ls public/
    artifacts:
        paths:
            - public
