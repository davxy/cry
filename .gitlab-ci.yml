image: davxy/sonar-builder

stages:
    - deploy

# Publish API Documentation
pages:
    stage: deploy
    script:
        - make doc
        - mv doc/doxy/html/* public/
    artifacts:
        paths:
            - public
    before_script:
        - ln -s /docs public
